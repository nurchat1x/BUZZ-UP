BUZZ-UP — ШПАРГАЛКА ДЛЯ НОВОГО ЧАТА (скопируй целиком)
====================================================
Обновлено: 24 июля 2026 — после чата про Render, камеру, Спринт 2 (звук + логи)

ПРОЕКТ
------
BUZZ-UP — детекция сонливости водителей + доведение до точки отдыха.
Папка: C:\Users\Lenovo\OneDrive\Desktop\copy.of.project
GitHub: https://github.com/nurchat1x/BUZZ-UP
Render (витрина): Docker Web Service из main
Streamlit Cloud: https://buzz-up-legendarnysait.streamlit.app (запасной, камера слабая)

⚠️ ЛОКАЛЬНЫЙ САЙТ — СВЯТОЙ. Идея и конcept автора. ИИ помогал с кодом.
   Все лучшие фичи сначала локально. Не ломать OpenCV Live ради облака.

СТЕК (актуальный)
-----------------
- app.py — главное Streamlit приложение
- drowsiness_detector.py — MediaPipe Face Landmarker + EAR
- alert_sound.py — звук при «Спит» (Web Audio API)
- fatigue_logger.py — логи в data/fatigue_log.jsonl
- bus_stops.json — маршруты, точки отдыха, рейтинги
- Dockerfile + render.yaml — деплой Render

ЛОКАЛЬНЫЙ ЗАПУСК (ГЛАВНЫЙ)
---------------------------
cd "C:\Users\Lenovo\OneDrive\Desktop\copy.of.project"
.\.venv\Scripts\activate
python -m streamlit run app.py
→ http://localhost:8501

⚠️ НЕ streamlit run app.py напрямую на Windows — Smart App Control блокирует .exe
⚠️ Если cv2/matplotlib DLL — python -m streamlit; в коде stub matplotlib для MediaPipe

РЕЖИМЫ КАМЕРЫ (локально)
-------------------------
1. OpenCV Live — РЕКОМЕНДУЕТСЯ. ▶ Включить камеру. Прямой доступ к веб-камере.
2. WebRTC — START у плеера. STUN только локально (без принудительного TURN).

На Render / облаке:
- WebRTC часто НЕ работает (STUN/TURN, сеть)
- Облачный UI: снимок st.camera_input, фото, видео
- is_cloud_deploy() = RENDER=true или STREAMLIT_SHARING_MODE

СПРИНТ 1 — СДЕЛАНО ✅
----------------------
✅ MediaPipe + EAR (без .pkl)
✅ streamlit-webrtc (video_processor)
✅ Карта pydeck, bus_stops.json с рейтингами
✅ OpenCV Live + WebRTC локально
✅ Render Docker (libgl1, libgles2, libegl1)
✅ Обход Windows matplotlib stub в drowsiness_detector.py
✅ Облачный fallback без падения при import cv2

СПРИНТ 2 — В ПРОЦЕССЕ
---------------------
✅ 1. Звуковой алерт — alert_sound.py, sidebar «Тревога»
✅ 2. Логи усталости — fatigue_logger.py, data/fatigue_log.jsonl
⬜ 3. Fleet dashboard — таблица/график из логов, демо-водители
⬜ 4. Сценарий скринкаста — ПИТЧ_СЦЕНАРИЙ.txt, demo mode

ЛОГИ УСТАЛОСТИ
--------------
Файл: data/fatigue_log.jsonl (в .gitignore, только локально)
События: drowsy_start, drowsy_continue, drowsy_end
Поля: time, session_id, EAR, status, nearest_stop, mode
Sidebar: вкл/выкл, пауза, просмотр сессии, скачать JSONL/CSV

ЗВУК
----
Sidebar: «Звук при сонливости», пауза 3–30 сек, «Проверить звук»
При «Спит» — сирена в браузере. Первый раз может нужен клик (autoplay policy).

ДЕПЛОЙ
------
Локаль — эталон.
Render: Runtime Docker, main branch, auto-deploy. Инструкция: ИНСТРУКЦИЯ_RENDER.txt
HF Docker — Paid (PRO). Gradio ZeroGPU — бесплатно, но другой UI.
Streamlit Cloud — карта + фото, live-камера ненадёжна.

packages.txt (Streamlit Cloud): libgl1
.python-version: 3.11

ИЗ ЭТОГО ЧАТА — ХРОНОЛОГИЯ
---------------------------
1. Streamlit Cloud: ImportError cv2 / libGLES — packages.txt, headless opencv
2. Windows: streamlit.exe заблокирован → python -m streamlit run app.py
3. matplotlib _image DLL → stub в drowsiness_detector.py
4. HF: Docker = Paid; выбрали Render
5. Render: сайт поднялся; WebRTC «Connection taking longer» → TURN только на облаке
6. Локально камера не работала → iceTransportPolicy relay ломал localhost
7. Fix: OpenCV Live + STUN-only локально → ОБА РЕЖИМА ЗАРАБОТАЛИ
8. Спринт 2: звук ✅, логи ✅

ФАЙЛЫ НЕ ТРОГАТЬ БЕЗ НУЖДЫ
--------------------------
train.py, infer.py — старый пайплайн (.pkl), не для app.py

КОМАНДЫ GIT
-----------
git add .
git commit -m "описание"
git pull origin main --rebase
git push origin main

СПРИНТ 3 (БУДУЩЕЕ)
------------------
- Пилот у перевозчика, Astana Hub
- Android app в кабине
- OSM/Overpass для точек отдыха
- MediaPipe в браузере (JS) — камера везде без WebRTC
- ИК-камера

ЦЕЛЕВАЯ АУДИТОРИЯ
-----------------
B2B: перевозчики, fleet, логистика, страховые
Фишка: не просто «разбудить», а «довести до точки отдыха»

СЛЕДУЮЩИЙ ШАГ В НОВОМ ЧАТЕ
---------------------------
Напиши: «Продолжаем Спринт 2 — fleet dashboard» или «ПИТЧ_СЦЕНАРИЙ»
Приложи эту шпаргалку целиком.

ИЗВЕСТНЫЕ ФИКСЫ
---------------
- mp.solutions нет → Tasks API FaceLandmarker
- video_transformer → video_processor
- st.session_state в WebRTC worker → зависание
- git push rejected → git pull origin main --rebase && git push
- HF token в git remote → отозвать на huggingface.co/settings/tokens
- BUZZUP_FORCE_WEBRTC=1 — принудить WebRTC UI на Render
- BUZZUP_FORCE_CLOUD_UI=1 — принудить облачный UI локально (тест)
