BUZZ-UP — ШПАРГАЛКА ДЛЯ НОВОГО ЧАТА (скопируй целиком)
====================================================

ПРОЕКТ
------
BUZZ-UP — детекция сонливости водителей.
Папка: C:\Users\Lenovo\OneDrive\Desktop\copy.of.project
GitHub: https://github.com/nurchat1x/BUZZ-UP
Streamlit Cloud: https://buzz-up-legendarnysait.streamlit.app

СТЕК (актуальный)
-----------------
- app.py — главное Streamlit приложение
- drowsiness_detector.py — MediaPipe Face Landmarker + EAR (НЕ старые .pkl)
- bus_stops.json — маршруты, точки отдыха, рейтинги
- streamlit-webrtc — камера в браузере
- MediaPipe >= 0.10.30 (Tasks API, НЕ mp.solutions — удалён в 0.10.30+)

ЛОКАЛЬНЫЙ ЗАПУСК (РАБОТАЕТ ОТЛИЧНО)
-----------------------------------
cd "C:\Users\Lenovo\OneDrive\Desktop\copy.of.project"
.\.venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
→ http://localhost:8501
Камера + EAR + очки + темнота — всё работает локально.

ЧТО СДЕЛАНО (СПРИНТ 1)
----------------------
✅ MediaPipe + EAR вместо Haar + Logistic Regression
✅ streamlit-webrtc (video_processor, не video_transformer)
✅ Карта: маркеры 50–80м (не 9000м!), zoom на GPS пользователя
✅ bus_stops.json обогащён рейтингами и удобствами
✅ Деплой на Streamlit Cloud (Python 3.11 обязательно!)

STREAMLIT CLOUD — КРИТИЧНЫЕ ПРАВИЛА
-----------------------------------
1. Python 3.11 при деплое (Advanced settings). 3.14 ломает cv2!
2. packages.txt — ТОЛЬКО libgl1-mesa-glx (НЕ libglib2.0-0 — ломает apt!)
3. .pkl модели удалены из git — не нужны
4. MediaPipe native libs на облаке часто НЕ работают → fallback: загрузка фото
5. Полная камера в реальном времени — только локально или будущее Android app

HUGGING FACE
------------
- Отдельного Streamlit SDK больше нет (с апреля 2025)
- Нужен Docker SDK (CPU basic free) ИЛИ просто Streamlit Cloud проще
- Старый Space Nurchat1x/BUZZ-UP можно удалить

ПЛАН НА БУДУЩЕЕ
---------------
Спринт 2: Fleet dashboard, логи усталости, звуковой алерт, скринкаст для питча
Спринт 3: Пилот у перевозчика, Astana Hub, Android app в кабине
Техника: OSM/Overpass, edge/offline, ИК-камера

ЦЕЛЕВАЯ АУДИТОРИЯ
-----------------
B2B: перевозчики, дальнобой, fleet, логистика, страховые
Фишка: не просто «разбудить», а «довести до точки отдыха»

ИЗВЕСТНЫЕ ОШИБКИ И ФИКСЫ
-----------------------
- mp.solutions нет → Tasks API FaceLandmarker
- video_transformer → video_processor (streamlit-webrtc 0.47+)
- st.session_state в WebRTC worker → зависание кадра
- git push rejected → git pull origin main --rebase && git push
- HF token в git remote → отозвать на huggingface.co/settings/tokens

ФАЙЛЫ НЕ ТРОГАТЬ БЕЗ НУЖДЫ
--------------------------
train.py, infer.py — старый пайплайн, для app.py не нужны

КОМАНДА ДЛЯ PUSH ПОСЛЕ ПРАВОК
------------------------------
git add .
git commit -m "описание"
git pull origin main --rebase
git push origin main
→ Streamlit Cloud пересоберёт сам
